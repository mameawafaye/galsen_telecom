name: galsen_telecom
description: Application marketplace télécom Galsen_Telecom
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Pour ajouter la police Poppins depuis Google Fonts, ajoutez ce package:
  # google_fonts: ^6.1.0
  # Et dans le ThemeData, utilisez:
  # fontFamily: GoogleFonts.poppins().fontFamily,